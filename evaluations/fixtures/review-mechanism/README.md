# Ledger review fixture

This fixture represents a pull request that adds a policy mechanism around the
small ledger artifact in `src/ledger.go`. The policy mechanism is intentionally
larger than the artifact and contains a concrete boundary defect for review.
