# Pull request policy

- Open one pull request per change issue; never duplicate an existing one.
- Validate locally before opening (`go test ./...` and the repository checks).
- Name the branch `issue/<number>`.