# Backlog triage fixture

This fixture contains a sanitized tracker snapshot and a small repository
snapshot. The tracker export is `issue-snapshot.md`. Current source evidence is
under `src/`, and merged-change evidence is in `merged-changes.md`.

The snapshot is synthetic. It contains no credentials, private URLs, personal
data, or real user transcript.
