#!/bin/sh
set -eu

if ! command -v mise >/dev/null 2>&1; then
  echo "mise is required to run repository checks." >&2
  echo "Run: mise run setup:all" >&2
  exit 2
fi

root=$(git rev-parse --show-toplevel)
exec bash "${root}/scripts/setup/run-mise.sh" run check:local
