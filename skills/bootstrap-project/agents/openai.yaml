interface:
  display_name: "Bootstrap Project"
  short_description: "Start projects with optional test rigor"
  default_prompt: "Use $bootstrap-project to set up a PR-only foundation, choose proportionate property or mutation checks, and record the FSL adoption plan."
