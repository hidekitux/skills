interface:
  display_name: "Debug Code"
  short_description: "Evidence-based debugging from reproduction to verified fix"
  default_prompt: "Use $debug-code to find the root cause of this failure and verify the fix."
